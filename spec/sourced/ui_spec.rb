# frozen_string_literal: true

RSpec.describe Sourced::UI do
  it "has a version number" do
    expect(Sourced::UI::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
